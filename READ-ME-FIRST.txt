Mktforge — Market Tracker: competitor tracking, round 2 (site files)
====================================================================

You already uploaded round 1 and published the Firestore rules. This is a
small follow-up: only ONE file actually changed.

1. COPY over the repo, keeping the paths:

       modules/market-tracker/market-tracker.js     90-day copy fix
       README.md                                    docs

   The other files in here (assets/js/data.js, firestore.rules,
   modules/market-tracker/market-tracker.css) are IDENTICAL to what you
   already uploaded. They're included so the folder is a complete set —
   copying them again is harmless.

   What changed: the page said "120 days" in two places (the empty-state line
   and the stats line). The deployed Worker has always used 90, so the page
   was telling users the wrong window. It now says 90.

2. DEPLOY THE WORKER. This is the part that makes Track Competitor work.

       cd C:\Users\Tyler\customer-tracker
       npx wrangler deploy

   src/index.js, src/competitor.js, src/shared.js and README.md have already
   been written to that folder. src/competitor.js and src/shared.js are new
   files; wrangler picks them up automatically (they're imported by index.js).

   Nothing else to configure. MAX_COMPETITOR_SEARCHES is optional and
   defaults to 12.

3. No Firestore change this round. Rules are already published.
