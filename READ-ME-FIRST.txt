Mktforge — Market Tracker: competitor tracking, round 2 (site files)
====================================================================

REPLACES the round-2 zip sent a few minutes ago. Two files changed this time,
not one.

1. COPY over the repo, keeping the paths:

       modules/market-tracker/market-tracker.js     picker markup + 90-day copy
       modules/market-tracker/market-tracker.css    picker spacing
       README.md                                    docs

   assets/js/data.js and firestore.rules are IDENTICAL to what you already
   uploaded and published. They're here so the folder is a complete set;
   copying them again is harmless, and the Firestore rules do NOT need
   re-publishing.

   What changed:
   - The drop-downs and their buttons nearly touched. Both rows now sit in
     one grid, so the two drop-downs start at the same x even though
     "Job Title" and "Competitor" are different widths, and there's 24px
     between each drop-down and its button. Under 560px wide the rows stack
     instead.
   - The page said "120 days" in two places. The Worker has always used 90,
     so the page was quoting the wrong window. It now says 90.

2. DEPLOY THE WORKER — this is what makes Track Competitor work at all.

       cd C:\Users\Tyler\customer-tracker
       npx wrangler deploy

   src/index.js, src/competitor.js, src/shared.js and README.md are already
   written to that folder. competitor.js and shared.js are new files;
   wrangler picks them up automatically.

3. No Firestore change. Rules are already published and unchanged.
