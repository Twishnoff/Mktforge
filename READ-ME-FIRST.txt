Mktforge — repo cleanup: four files landed at the root
======================================================

Nothing here is new code. These are the files you already uploaded, in the
folders they belong in.

WHAT HAPPENED

Five files were uploaded to the repo root instead of into their folders:

    /data.js               /extract.js            /market-tracker.js
    /opportunities-pdf.js  /persona-pdf.js

index.html loads assets/js/data.js and assets/js/extract.js, not the root
copies, so the root files are doing nothing and the app is still running the
OLD versions. Right now the live site has no title ledgers, no links-aware
text, and Marketing Opportunities still prints "Visit Site".

market-tracker.js is the exception — the copy in modules/market-tracker/ is
already correct. Its root twin is just a stale duplicate.

FIX — 1. UPLOAD these four, into these folders:

    assets/js/data.js
    assets/js/extract.js
    modules/marketing-opportunities/opportunities-pdf.js
    modules/persona-builder/persona-pdf.js

On github.com: open the target FOLDER first, then Add file -> Upload files.
Uploading from the repo root drops the file at the root, which is how this
happened.

FIX — 2. DELETE all five root files:

    /data.js
    /extract.js
    /market-tracker.js
    /opportunities-pdf.js
    /persona-pdf.js

Open each one, trash icon, commit.

No Worker deploy. No Firestore change.
